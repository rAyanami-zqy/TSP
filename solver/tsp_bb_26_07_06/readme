# 版本概览
1.BP（Branch Partitioning）选边 + 标准二分分支：
    - BP 负责边选择（不做关键性测试），二分分支保证完备性
    选边规则（二级优先）：
        Priority 1: 1-tree 中度 > 2 的顶点，选其关联的最小权重候选边
                    直接针对 tour 条件违反点，分支子问题更紧
        Priority 2: 1-tree 中全局最小权重非固定边（fallback）
    分支规则（标准二分，完备性来源）：
        - Force B[0] → 覆盖 B[0] ∈ opt 的情况
        - Forbid B[0] → 覆盖 B[0] ∉ opt 的情况
        - 两个分支穷尽所有可能，forbid 子节点重新执行 bpPartition 选边
    完备性保证：
        - 完备性由二叉树的穷举性保证，与 BP 理论的关键性保证无关
        - BP 选边仅作为启发式提高搜索效率，不影响完备性
        - 旧版链式分支依赖 BP 理论的 B 集完备性，导致 |B|-叉树爆炸

2.命令行入口：
    ./tsp_bb [options] <instance-file>    （无需 --branch-strategy 参数）

3.代码清理：
    - 移除 chain_branch 链式分支（|B|-叉树 → 二叉树）
    - 移除 bpPartition 中的关键性测试（forbid→重算 1-tree→检查下界）
      bpPartition 现仅做选边，不做 1-tree 重算
    - 移除 BranchStrategy 枚举及 Smart/Simple 旧策略代码
    - 旧版 Smart 的 chooseBranchEdge() Priority-1 逻辑整合进 bpPartition

4.2026-07-06 架构演变（四轮）：
    第一轮：编译修复 — bpPartition 签名不匹配
    第二轮：forbid_trees 复用 — forbid 分支避免重算 computeOneTree
    第三轮：发现 |B|-叉树爆炸 — fri26 720K→1.6M 节点, wi29 4.8M 节点
    第四轮：回归标准二分 + 简化 bpPartition
        - 根因：链式分支对每个 B 集边创建 force 分支（|B|-叉树）
        - 修复：标准二分分支（二叉树），bpPartition 仅选边不做测试
        - 完备性由二叉树穷举保证，不依赖 BP 理论的关键性枚举

5.性能（最终版 ≈ 旧版 Smart，同节点数，架构更清晰）：
    | Instance | 旧版 smart (02) | 最终版 BP |
    |----------|-----------------|------------|
    | burma14  | 26ms, 9.1K     | 20ms, 9.1K  |
    | gr17     | 2.2s, 920K     | 2.3s, 920K  |
    | fri26    | 1.9s, 492K     | 2.1s, 492K  |
    | wi29     | 201ms, 38K     | 220ms, 38K  |
    | ulysses16| 16.1s, 7.8M    | 16.6s, 7.8M |
