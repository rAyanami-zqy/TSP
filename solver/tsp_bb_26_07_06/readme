# tsp_bb 2026-07-06 版本说明

本说明按代码重写，不沿用旧 readme 的结论。当前目录中的
`tsp_bb` 对应 Git 提交 `0b2fae2` 中更新的二进制，文件类型是
Linux x86-64 ELF 可执行文件。

旧 readme 中“回归标准二分分支”“bpPartition 仅选边、不做关键性测试”等说法
与该版本代码不一致。这个版本实际仍是 BP-chain 分支定界版本。

## 命令行

```bash
./tsp_bb [options] [matrix-or-tsplib-file]
./tsp_bb [options] --batch <list-file>
```

支持的选项：

- `--batch <list-file>`：批处理清单，每行一个实例路径，空行和 `#` 开头的行会被忽略。
- `--exact-max-n <n>`：转换成稠密矩阵前的规模上限，默认 `10000`。
- `--debug`：向 stderr 输出求解过程调试信息。
- `--debug-interval <n>`：调试进度输出间隔，默认 `10000`。
- `--help` / `-h`：打印帮助。

该版本没有 `--method`、`--branch-strategy` 等选项；CLI 总是走精确
`BranchBoundSolver`，不会自动切换到启发式求解。

## 输入与输出

支持两类输入：

1. 本项目矩阵格式：第一项是顶点数 `n`，后面是 `n*n` 个距离。
   缺边可以写成 `inf`、`infinity`、`-` 或 `x`。
2. TSPLIB `TSP` 格式：支持坐标型 `NODE_COORD_SECTION` 和显式矩阵
   `EDGE_WEIGHT_SECTION`。

坐标距离类型支持：

- `EUC_2D`
- `CEIL_2D`
- `FLOOR_2D`
- `MAN_2D`
- `MAX_2D`
- `EUC_3D`
- `CEIL_3D`
- `MAN_3D`
- `MAX_3D`
- `ATT`
- `GEO`

显式矩阵格式支持：

- `FULL_MATRIX`
- `UPPER_ROW`
- `UPPER_DIAG_ROW`
- `LOWER_ROW`
- `LOWER_DIAG_ROW`
- `UPPER_COL`
- `UPPER_DIAG_COL`
- `LOWER_COL`
- `LOWER_DIAG_COL`

求解器要求：

- 顶点数至少为 3。
- 边权非负，缺边用 infinity 表示。
- 距离矩阵必须对称；非对称实例会报错。

单实例输出包含：

- `Problem`
- `Dimension`
- `Method: exact`
- `Root lower bound`
- `Initial upper bound`
- `Nodes created`
- `Nodes expanded`
- `Pruned by bound`
- `Pruned infeasible`
- `Optimal cost`
- `Tour`

批处理输出 CSV：

```text
instance,status,method,dimension,cost,root_lower_bound,initial_upper_bound,nodes_created,nodes_expanded,pruned_by_bound,pruned_infeasible,tour,message
```

注意：该版本的解析代码残留了 `std::cout << "debug:" ...`。读取矩阵时会输出
`debug:<n>`，读取 TSPLIB 时会输出各行大写后的 `debug:` 信息，读取显式权重段时
还会输出 `debug:EdgeWeight`。这些内容写到 stdout，会污染单实例输出和批处理 CSV。

## 实际算法

主流程：

1. 读取矩阵或 TSPLIB，并转换成稠密对称距离矩阵。
2. 用最近邻生成初始回路：从每个顶点作为起点尝试一次。
3. 对最佳最近邻回路做完整 `2-opt`，再做 Lin-Kernighan 改进。
4. 初始化所有有限无向边为候选边，按权重升序排序。
5. 用受约束 1-tree 作为下界，进入 BP-chain 分支定界搜索。

初始上界：

- 最近邻遍历所有起点。
- `2-opt` 持续改进到无下降交换。
- LK 使用每个顶点 8 个近邻候选，最大搜索深度 5。
- Chained LK 最多做 10 次 deterministic double-bridge kick。

1-tree 下界：

- 顶点 `1..n-1` 上用 Kruskal 构造 MST。
- 顶点 `0` 额外接入两条最短可用 incident edge。
- `forced` 边会直接进入 1-tree；`forbidden` 边会被排除。
- 若 1-tree 有 `n` 条边且所有顶点度数都是 2，则该 1-tree 被视为 Hamilton 回路。

搜索节点状态：

- `forced`：必须选的边。
- `forbidden`：禁止选的边。
- forced 边的可回溯并查集、度数、连通分量大小。
- forced MST 部分的累计成本和边数。
- 当前候选边掩码 `candidate_mask`。

## BP-chain 分支逻辑

每个 `search()` 节点先计算当前 1-tree：

- 若下界不优于当前上界，按 bound 剪枝。
- 若当前 1-tree 已经是 tour，更新 incumbent。
- 否则调用 `bpPartition()` 生成 B 集。

`bpPartition()` 的真实行为：

1. 在当前工作 1-tree 上选边。
2. 优先选 degree 大于 2 的顶点中，关联的最小权重未决边。
3. 若度数优先边测试失败，再测试当前 1-tree 中全局最小权重未决边。
4. 测试方式是临时禁止该边、从候选集中删除该边、重算 1-tree。
5. 若新 1-tree 不可行，该边不进入 B 集，并撤销禁止。
6. 若新 1-tree 的 cost 大于当前 best upper bound，该边视为安全边，不进入 B 集，
   并撤销禁止。
7. 若新 1-tree 仍可行且 cost 不超过当前 best，该边进入 B 集；该 forbidden
   状态在 `bpPartition()` 的工作副本中保留，并继续下一轮划分。
8. `forbid_trees[i]` 保存禁止 `B[i]` 后重算得到的 1-tree，供后续 forbid 链复用。

`search()` 对 B 集做链式分支：

- Force `B[i]`：强制该边，过滤候选集，递归调用完整 `search()`。
- Forbid `B[i]`：禁止该边，移除候选边，只做轻量可行性检查，然后使用
  `forbid_trees[i]` 做下界剪枝；如果未剪枝，则继续处理 `B[i+1]`。
- Forbid 链不会重新调用 `search()`，也不会重新执行新的 BP 划分。
- 所有 B 边都被 forbid 后，链结束并返回。

因此，这个版本不是标准二分分支版本；它依赖 B 集链式分支和
`forbid_trees` 复用。

## 已知限制

- 二进制是 Linux x86-64 ELF，不是 macOS arm64 可执行文件。
- stdout 中存在解析阶段的 `debug:` 残留输出，会影响 CSV 后处理。
- CLI 没有启发式模式；`solveHeuristic()` 虽在代码中存在，但 `main.cpp` 不调用。
- 该版本的 forbid 链只用预计算 `forbid_tree` 做剪枝，不会在该处检查
  `forbid_tree` 是否已经是 tour 并更新 incumbent；后续版本对此做过修正。
- BP 选边仍包含“全局最小未决边 fallback”，后续版本的 BP 终止条件与选边逻辑
  已有变化。

