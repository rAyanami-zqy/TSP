# tsp_bb 2026-07-14 deg

这是当前源码的默认/主配置，也是 `tsp_bb` 对应的 `07_14` 版本：degree 分支策略 + 增量 1-tree。

## 配置

- 分支策略：从 1-tree 中选择度数大于 2 的违规顶点，再取该顶点关联的最轻未决树边。
- 1-tree：子节点优先继承父节点 1-tree，并对被移除的树边做增量 replacement。
- 安全回退：增量结果不能满足当前 forced/forbidden 约束或结构校验时，从当前节点约束完整重建 1-tree。
- 编译宏：均不定义（`TSP_BRANCH_STRATEGY_MIN_EDGE` 与 `TSP_DISABLE_INCREMENTAL_ONETREE` 都关闭）。

四个 `07_14` 变体共享 `src/TspSolver.cpp`，目录中不保存独立源码。

## 可复现源码提交

- 源码提交：`313143543b29a3ed469e5296f5356b4ddc12109d`
- 提交说明：`Fix 07_14 BP correctness and reproducible builds`
- CMake 目标：`tsp_bb_26_07_14_deg`（默认目标 `tsp_bb` 与它配置相同）

在其他系统编译前，先确认工作树基于该提交：

```bash
git checkout --detach 313143543b29a3ed469e5296f5356b4ddc12109d
git rev-parse HEAD
```

第二条命令必须输出上述完整 hash。若该提交尚未出现在目标系统的仓库中，需要先从包含它的远端执行 `git fetch`。

## 从固定提交构建

构建默认目标：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --target tsp_bb
```

构建带版本名的同配置目标：

```bash
cmake --build build --target tsp_bb_26_07_14_deg
```

生成物分别是 `build/tsp_bb` 和 `build/tsp_bb_26_07_14_deg`。本目录中的 `tsp_bb` 是历史归档产物，并非上述提交在当前系统上的构建结果；验证时应重新构建。

## 正确性验证

```bash
cmake -S . -B build -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Release
cmake --build build --target tsp_solver_tests
ctest --test-dir build --output-on-failure
```

测试目标使用本配置，并通过 `TSP_VERIFY_INCREMENTAL_STATE` 将搜索中复用的增量 1-tree 与完整重建结果对比。测试还覆盖 BP 前缀约束、已知 5 点错解、极小权重和固定随机种子的完整图/稀疏图穷举对拍。这里不再引用无法由当前测试命令复现的历史通过次数。
