tsp_bb 2026-07-14 full-deg（对照版本：全量重算 1-tree）

## 代码基

e6a862d — Fix prefix-forbidden edge leak in child 1-tree, add 07_13 variants

## 分支策略

kUseMinEdgeFromOneTree = false（degree-violation vertex only）

## 候选集管理

与 deg 版本相同：buildBranchCandidates 全量 O(n²) 重建（resize 方式）。

## 1-tree 维护方式

**全量重算**（每个子节点从零构建 1-tree）

- 子节点不继承父节点 1-tree
- search() 始终传 nullptr 给子节点
- 子节点进入 search 后调用 computeOneTree 全量 Kruskal + forced edges 重建 1-tree
- 不使用 updateOneTreeAfterCandidateRemoval 增量更新
- 不维护 prefix_tree

## 与 deg 版本的唯一差异

```
deg 版本:   search(node, ..., tree_valid ? &child_tree : nullptr);
full 版本:  search(node, ..., nullptr);
```

其余代码完全相同。

## Replacement 方法

与 deg 版本相同：位图邻接矩阵 + DFS + epoch 标记。

## BP 划分

与 deg 版本相同。

## 用途

对照实验：量化增量 1-tree 维护 vs 全量重算对搜索树大小的影响。

## 编译

g++ via CMake/Make, Linux x86_64
