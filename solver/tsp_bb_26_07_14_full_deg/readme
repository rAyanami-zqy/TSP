# tsp_bb 2026-07-14 full-deg

这是 `07_14` 的对照配置：degree 分支策略 + 每个子节点完整重建 1-tree。

## 分支

- 开发分支：`full`
- 当前提交：`f4f34e5`
- 优化：search() 中跳过未使用的前缀 1-tree 完整重建（bpPartition 已完成 forbid 前缀的 feasible/bound 验证，force 子节点始终独立重建）

## 配置

- 分支策略：与默认 `deg` 相同，从度数大于 2 的违规顶点选择最轻未决树边。
- 1-tree：禁用父树增量复用；每个子节点依据当前 forced/forbidden 约束完整执行受约束 Kruskal 重建。
- 编译宏：定义 `TSP_DISABLE_INCREMENTAL_ONETREE=1`，不定义 `TSP_BRANCH_STRATEGY_MIN_EDGE`。

该配置用于隔离增量维护对性能的影响，与默认配置共享同一份源码。

## 可复现源码提交

- 源码提交：`313143543b29a3ed469e5296f5356b4ddc12109d`
- 提交说明：`Fix 07_14 BP correctness and reproducible builds`
- CMake 目标：`tsp_bb_26_07_14_full_deg`

在其他系统编译前，先确认工作树基于该提交：

```bash
git checkout --detach 313143543b29a3ed469e5296f5356b4ddc12109d
git rev-parse HEAD
```

第二条命令必须输出上述完整 hash。若该提交尚未出现在目标系统的仓库中，需要先从包含它的远端执行 `git fetch`。

## 从固定提交构建

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --target tsp_bb_26_07_14_full_deg
```

生成物是 `build/tsp_bb_26_07_14_full_deg`。本目录中的 `tsp_bb` 是历史归档产物，并非上述提交在当前系统上的构建结果；验证时应重新构建。

一次构建四个实验变体可运行：

```bash
cmake --build build --target tsp_bb_variants
```

## 正确性验证

默认 CTest 目标覆盖主 `deg + incremental` 配置，并在验证模式下用本配置所代表的完整重建结果检查增量树。也可直接用本目标配合独立 Held-Karp 校验：

```bash
python3 tools/verify_instances.py --batch-list examples/batch.txt \
  --solver ./build/tsp_bb_26_07_14_full_deg
```
