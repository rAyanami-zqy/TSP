# tsp_bb 2026-07-13-01 版本说明

本目录中的 `tsp_bb` 对应 Git 提交 `db6fa0e`，文件类型是 Linux x86-64 ELF 可执行文件。

该版本在 BP 选边策略中使用 `kUseMinEdgeFromOneTree = true`，即从 1-tree 所有未决边中直接选最小权重边。

## 命令行

```bash
./tsp_bb [options] [matrix-or-tsplib-file]
./tsp_bb [options] --batch <list-file>
```

支持的选项：

- `--batch <list-file>`：批处理清单，每行一个实例路径，空行和 `#` 开头的行会被忽略。
- `--exact-max-n <n>`：转换成稠密矩阵前的规模上限，默认 `10000`。
- `--debug`：向 stderr 输出求解过程调试信息。
- `--debug-interval <n>`：调试进度输出间隔，默认 `10000`。
- `--help` / `-h`：打印帮助。

## 选边策略

- `kUseMinEdgeFromOneTree = true`：从 1-tree 所有未决边中直接选最小权重边（新策略）

## 已知限制

- 二进制是 Linux x86-64 ELF。
- CLI 没有启发式模式。
