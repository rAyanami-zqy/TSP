# 版本概览
1.BP（Branch Partitioning）链式分支版本：
    - 每个展开节点先执行 bpPartition，生成 B_set 和对应 forbid_trees
    - 对 B_set 做链式 DFS：
        Force B[i] -> 递归 search
        Forbid B[i] -> 沿链进入 B[i+1]，不调用完整 search

2.当前 BP 选边规则：
    Priority 1: 在当前 1-tree 中找度数 > 2 的最大度顶点，
                选该顶点关联的最小权重未决边
    Priority 2: 若 Priority 1 无法继续，则选当前 1-tree 中全局最小权重未决边
    每轮最多测试 2 条边。

3.本版关键修正：
    - 选中的测试边先加入 B_set，再计算 forbid 后的 1-tree
    - forbid 后不可行：该边仍属于 B，停止 BP 划分
    - forbid 后 bound 可剪：该边仍属于 B，停止 BP 划分
    - forbid 后仍可行且未被 bound 剪：保留 forbidden 状态，更新 work_tree 后继续划分
    - bpPartition 内部使用 shouldPrune(t.cost, best_cost_)，与主搜索剪枝口径一致
    - 链式 forbid 分支会正确处理 infeasible forbid_tree，计入 pruned_infeasible

4.命令行入口：
    ./tsp_bb [options] <instance-file>
    ./tsp_bb [options] --batch <list-file>

5.复测结果：
    | Instance | Cost | Nodes Created | Nodes Expanded |
    |----------|------|---------------|----------------|
    | five-city.txt | 26 | 13 | 5 |
    | wi29.tsp | 27603 | 77844 | 22413 |
    | gr21.tsp | 2707 | 34885 | 10118 |

6.说明：
    - wi29 相比上一版链式 BP 展开节点明显下降
    - gr21 相比上一版链式 BP 展开节点上升，说明该 BP 划分策略仍存在实例敏感性
