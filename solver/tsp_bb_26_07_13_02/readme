tsp_bb 2026-07-13-02 (correctness repair)

Base source: e6a862d Fix prefix-forbidden edge leak in child 1-tree

Strategy: kUseMinEdgeFromOneTree = false (degree-violation vertex only)

Changes from 07-08-02:
- Pass parent 1-tree to children, incrementally update instead of recomputing MST
- Add updateOneTreeAfterCandidateRemoval for incremental tree updates
- Fix: prefix-forbidden B-set edges properly removed from child's 1-tree
- Fallback to full computeOneTree when no replacement exists

Correctness repair:
- Validate that a reused child 1-tree contains the edge forced by the child
- Fall back to a full constrained 1-tree rebuild when a B[1+] edge is missing

Validation:
- Complete and sparse BP regression cases both return the optimum 1920
- 36 complete/sparse random instances (n=5..8) matched brute force

Build: AppleClang via CMake, macOS arm64
